import { PrimaryColors } from "@src/utilities";
import styled from "styled-components";

export const StyledSummaryUnit = styled.div`
    padding: 0 10px 0 10px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: opacity .4;
    background-color: ;

    .info{
        display: flex;
        align-items: center;
        gap: 5px;
    }

    .score{

    }
`